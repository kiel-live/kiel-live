type Map {
  stops: [Stop!]!
  vehicles: [Vehicle!]!
}

type Subscription {
  map(minLat: Float!, minLng: Float!, maxLat: Float!, maxLng: Float!): Map!
  stop(id: ID!): Stop!
  vehicle(id: ID!): Vehicle!
  trip(id: ID!): Trip!
  route(id: ID!): Route!
}
