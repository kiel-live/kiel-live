{
  "name": "@kiel-live/commons",
  "version": "2.0.0",
  "main": "dist/",
  "types": "dist/",
  "scripts": {
    "start": "tsc --watch",
    "build": "yarn clean && tsc",
    "lint": "eslint --report-unused-disable-directives --cache --ext .js,.ts .",
    "typecheck": "tsc --noEmit",
    "clean": "shx rm -rf dist/"
  },
  "devDependencies": {
    "shx": "0.3.3"
  }
}
