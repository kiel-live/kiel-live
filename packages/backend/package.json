{
  "name": "@kiel-live/backend",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon --watch src src/",
    "build": "yarn clean && tsc",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "typecheck": "tsc --noEmit",
    "clean": "shx rm -rf dist/"
  },
  "dependencies": {
    "axios": "0.21.1",
    "express": "4.17.1",
    "flat-cache": "3.0.4",
    "geolocation-utils": "1.2.3",
    "querystring": "0.2.0",
    "socket.io": "2.3.0"
  },
  "devDependencies": {
    "nodemon": "2.0.6"
  }
}
