{
  "name": "@kiel-live/server",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon",
    "build": "yarn clean && esbuild src/index.ts --bundle --platform=node --outdir=dist/",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "typecheck": "tsc --noEmit",
    "clean": "shx rm -rf dist/"
  },
  "dependencies": {
    "@kiel-live/commons": "*",
    "axios": "0.21.1",
    "express": "4.17.1",
    "flat-cache": "3.0.4",
    "geolocation-utils": "1.2.3",
    "querystring": "0.2.0",
    "socket.io": "3.0.4"
  },
  "devDependencies": {
    "esbuild": "0.8.27",
    "esbuild-node-tsc": "1.0.5",
    "nodemon": "2.0.6"
  }
}
